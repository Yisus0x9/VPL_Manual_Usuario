
%  Manual de Usuario — Laboratorio de Programación Virtual
%  para el Diseño Lógico Digital mediante Lógica Reconfigurable

\documentclass[12pt, a4paper, twoside, openany]{book}

% ── Codificación y lenguaje ─────────────────────────────────
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[spanish]{babel}

% ── Tipografía elegante ─────────────────────────────────────
\usepackage{lmodern}
\usepackage{microtype}

% ── Geometría de página ─────────────────────────────────────
\usepackage[
  top=3cm, bottom=3cm,
  inner=3.5cm, outer=2.5cm,
  headheight=15pt
]{geometry}

% ── Color y gráficos ────────────────────────────────────────
\usepackage[dvipsnames, table]{xcolor}
\usepackage{graphicx}
\usepackage{tikz}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}

% Paleta principal
\definecolor{PrimaryBlue}{RGB}{0, 61, 124}
\definecolor{AccentBlue}{RGB}{0, 120, 215}
\definecolor{LightGray}{RGB}{245, 247, 250}
\definecolor{DarkGray}{RGB}{60, 60, 60}
\definecolor{SuccessGreen}{RGB}{34, 139, 34}
\definecolor{WarnOrange}{RGB}{204, 102, 0}

% ── Encabezados y pies de página ────────────────────────────
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\fancyhead[LE]{\small\color{PrimaryBlue}\leftmark}
\fancyhead[RO]{\small\color{PrimaryBlue}\rightmark}
\fancyfoot[C]{\small\color{DarkGray}\thepage}
\renewcommand{\headrulewidth}{0.4pt}
\renewcommand{\headrule}{\color{AccentBlue}\hrule width\headwidth height\headrulewidth}

% ── Capítulos y secciones (titlesec) ────────────────────────
\usepackage{titlesec}
\usepackage{titletoc}

\titleformat{\chapter}[display]
  {\normalfont}
  {\raggedleft\fontsize{72}{72}\selectfont\color{DarkGray!18}\thechapter}
  {-14pt}
  {\huge\bfseries\color{DarkGray}}
  [{\vspace{6pt}\color{AccentBlue}\titlerule[1pt]}]
\titlespacing*{\chapter}{0pt}{20pt}{32pt}

\titleformat{\section}
  {\normalfont\Large\bfseries\color{DarkGray}}
  {\thesection}{1em}{}
  [{\vspace{2pt}\color{DarkGray!30}\titlerule[0.5pt]}]

\titleformat{\subsection}
  {\normalfont\large\bfseries\color{DarkGray}}
  {\thesubsection}{1em}{}

\titleformat{\subsubsection}
  {\normalfont\normalsize\bfseries\itshape\color{DarkGray}}
  {\thesubsubsection}{1em}{}

% ── Listas y enumeraciones ──────────────────────────────────
\usepackage{enumitem}
\setlist[itemize,1]{label=\textcolor{AccentBlue}{\textbullet}}
\setlist[itemize,2]{label=\textcolor{PrimaryBlue}{\textendash}}

% ── Cajas y avisos ──────────────────────────────────────────
\usepackage[most]{tcolorbox}

\tcbset{
  base style/.style={
    enhanced, arc=4pt, boxrule=0pt,
    left=6pt, right=6pt, top=6pt, bottom=6pt,
    fonttitle=\bfseries\small,
  }
}

\newtcolorbox{notebox}[1][Nota]{
  base style,
  colback=AccentBlue!10, colframe=AccentBlue,
  title={#1}, attach boxed title to top left={xshift=8pt, yshift=-4pt},
  boxed title style={colback=AccentBlue, arc=3pt, boxrule=0pt}
}

\newtcolorbox{warnbox}[1][Advertencia]{
  base style,
  colback=WarnOrange!10, colframe=WarnOrange,
  title={#1}, attach boxed title to top left={xshift=8pt, yshift=-4pt},
  boxed title style={colback=WarnOrange, arc=3pt, boxrule=0pt}
}

\newtcolorbox{tipbox}[1][Consejo]{
  base style,
  colback=SuccessGreen!8, colframe=SuccessGreen,
  title={#1}, attach boxed title to top left={xshift=8pt, yshift=-4pt},
  boxed title style={colback=SuccessGreen, arc=3pt, boxrule=0pt}
}

% Paso numerado (para procedimientos)
\newtcolorbox{stepbox}[1]{
  base style,
  colback=LightGray, colframe=PrimaryBlue, boxrule=1pt,
  title={Paso #1}, fonttitle=\bfseries\color{white},
  attach boxed title to top left={xshift=8pt, yshift=-4pt},
  boxed title style={colback=PrimaryBlue, arc=3pt}
}

% ── Código fuente ───────────────────────────────────────────
\usepackage{listings}
\lstset{
  backgroundcolor=\color{LightGray},
  basicstyle=\ttfamily\small\color{DarkGray},
  keywordstyle=\bfseries\color{PrimaryBlue},
  commentstyle=\itshape\color{SuccessGreen!70!black},
  stringstyle=\color{WarnOrange},
  numberstyle=\tiny\color{DarkGray!60},
  numbers=left, numbersep=8pt,
  breaklines=true, keepspaces=true,
  frame=single, framerule=0.4pt, rulecolor=\color{AccentBlue!40},
  xleftmargin=15pt, xrightmargin=5pt,
  captionpos=b,
  showstringspaces=false,
}

% Lenguaje VHDL básico
\lstdefinelanguage{VHDL}{
  morekeywords={
    entity, architecture, port, in, out, inout, buffer, signal,
    process, begin, end, if, then, else, elsif, case, when,
    std_logic, std_logic_vector, integer, boolean,
    library, use, all, is, of, downto, to, others,
    component, map, generic, wait, rising_edge, falling_edge
  },
  morecomment=[l]{--},
  morestring=[b]{"},
  sensitive=false
}

% ── Hipervínculos ───────────────────────────────────────────
\usepackage[
  colorlinks=true,
  linkcolor=PrimaryBlue,
  urlcolor=AccentBlue,
  citecolor=DarkGray,
  bookmarks=true,
  bookmarksnumbered=true,
  pdftitle={Manual de Usuario — Laboratorio de Programación Virtual},
  pdfauthor={Treviño Hernández Jonathan; Peñarrieta Villa Jesús},
  pdfsubject={VPL · VHDL · Lógica Digital Reconfigurable}
]{hyperref}

\usepackage{bookmark}

% ── Tablas ──────────────────────────────────────────────────
\usepackage{booktabs}
\usepackage{array}
\usepackage{longtable}
\newcolumntype{L}[1]{>{\raggedright\arraybackslash}p{#1}}
\newcolumntype{C}[1]{>{\centering\arraybackslash}p{#1}}

% ── Misceláneos ─────────────────────────────────────────────
\usepackage{emptypage}
\usepackage{float}
\usepackage{caption}
\captionsetup{font=small, labelfont={bf,color=PrimaryBlue}}

% Evita que LaTeX estire el espacio vertical para justificar el pie de
% página (\flushbottom por defecto en book): el sobrante se acumula al
% final de la página en lugar de generar saltos grandes entre bloques.
\raggedbottom


%  Macros de utilidad

\newcommand{\vpl}{\textsc{VPL}\xspace}
\newcommand{\vhdl}{\textsc{VHDL}\xspace}
\newcommand{\ssh}{\textsc{SSH}\xspace}
\newcommand{\moodle}{\textit{Moodle}\xspace}
\newcommand{\archivo}[1]{\texttt{\small#1}}
\newcommand{\menu}[1]{\textsf{\textbf{#1}}}

% ── Iconos de acciones del editor (inline) ──────────────────
% \icono coloca el icono PNG en el mismo renglón, alineado con el texto.
% \accion combina icono + etiqueta en negrita para las acciones de la barra.
\newcommand{\icono}[1]{%
  \raisebox{-0.25em}{\includegraphics[height=1.1em]{iconos/#1}}}
\newcommand{\accion}[2]{\icono{#1}\,\menu{#2}}

\usepackage{xspace}


%  DOCUMENTO

\begin{document}

% ── Portada ─────────────────────────────────────────────────
\begin{titlepage}
  \pagecolor{PrimaryBlue}
  \color{white}
  \centering
  \vspace*{1.5cm}

  % Logo o símbolo decorativo
  \begin{tikzpicture}
    \draw[white, line width=2pt] (0,0) rectangle (3,3);
    \draw[AccentBlue!60, line width=1.2pt] (0.3,0.3) rectangle (2.7,2.7);
    \node[white, font=\Huge\bfseries] at (1.5,1.5) {VPL};
  \end{tikzpicture}

  \vspace{1cm}
  {\fontsize{11}{13}\selectfont\scshape Laboratorio de Programación Virtual}\\[0.4cm]
  {\fontsize{11}{13}\selectfont\scshape para el Diseño Lógico Digital}\\[0.4cm]
  {\fontsize{11}{13}\selectfont\scshape mediante Lógica Reconfigurable}\\[1cm]

  {\color{AccentBlue!60}\rule{0.6\textwidth}{1pt}}\\[0.8cm]

  {\Huge\bfseries Manual de Usuario}\\[0.5cm]

  {\color{AccentBlue!60}\rule{0.6\textwidth}{1pt}}\\[2cm]

  {\large
    Treviño Hernández Jonathan\\[0.3cm]
    Peñarrieta Villa Jesús
  }\\[2.5cm]

  {\small\color{white!70} Versión 1.0 \textendash{} \the\year}

  \vfill
  {\small\color{white!60}
    Escuela Superior de Cómputo\\
    Ingeniería en Sistemas Computacionales
  }
  \vspace{1cm}
\end{titlepage}

\nopagecolor  % restaurar fondo blanco

% ── Página de créditos ──────────────────────────────────────
\thispagestyle{empty}
\vspace*{\fill}
\begin{center}
  \small\color{DarkGray}
  \textbf{Manual de Usuario}\\
  Laboratorio de Programación Virtual para el Diseño Lógico Digital\\
  mediante Lógica Reconfigurable\\[1em]
  \textit{Treviño Hernández Jonathan \textendash{} Peñarrieta Villa Jesús}\\[1em]
  \the\year\\[2em]
  Este documento fue elaborado con fines académicos.\\
  Plataforma: Moodle + Virtual Programming Lab (VPL)
\end{center}
\vspace*{\fill}
\clearpage

% ── Tabla de contenidos ─────────────────────────────────────
\frontmatter
\tableofcontents
\listoffigures
\listoftables
\clearpage

% ── Prefacio ────────────────────────────────────────────────
\chapter*{Prefacio}
\addcontentsline{toc}{chapter}{Prefacio}

Este manual describe el uso del \textbf{Laboratorio de Programación Virtual}
(\vpl) integrado en \moodle para el diseño y verificación de circuitos
digitales escritos en \vhdl mediante lógica reconfigurable.

El documento se divide en tres partes independientes:

\begin{itemize}
  \item \textbf{Parte I — Guía del Alumno:} acceso, edición, compilación,
        simulación y evaluación automática de diseños \vhdl dentro del entorno \vpl.
  \item \textbf{Parte II — Guía del Profesor:} creación y configuración de
        actividades \vpl, definición de casos de prueba, generación de
        \textit{testbenches} y análisis de reportes de evaluación.
  \item \textbf{Parte III — Guía del Servicio de Generación de Bitstream:}
        uso del servicio remoto que toma un diseño \vhdl, asigna los pines
        físicos de una tarjeta FPGA y genera el archivo de configuración
        \texttt{.bit} junto con su archivo de restricciones \texttt{.xdc},
        incluyendo su programación y prueba en el laboratorio remoto de
        FPGA's.
\end{itemize}

\begin{notebox}[Sobre este documento]
  Se recomienda leer la documentación oficial de VPL for Moodle para saber más sobre sus características avanzadas, personalización y solución de problemas comunes: \url{https://vpl.dis.ulpgc.es/documentation/vpl-4.4.2/}
\end{notebox}

\clearpage

%           PARTE I — GUÍA DEL ALUMNO
\mainmatter

\part{Guía del Alumno}

% ── Capítulo 1 ──────────────────────────────────────────────
\chapter{Acceder a la Actividad VPL}
\label{ch:acceso}

\section{Requisitos previos}
\label{sec:requisitos}

Para utilizar el laboratorio virtual \vpl es necesario contar con:

\begin{itemize}
  \item \textbf{Navegador web actualizado:} se recomienda Chrome, Firefox,
        Brave o Edge. 
  \item \textbf{Cuenta activa en Moodle:} el alumno debe tener usuario y
        contraseña válidos para iniciar sesión en la plataforma.
  \item \textbf{Estar inscrito en el curso:} la cuenta debe estar inscrita en el
        curso que contiene la actividad \vpl.
  \item \textbf{Conexión a internet estable:} la cconexión al lab remoto y la descarga de archivos se
        ejecutan en el servidor; una conexión inestable puede interrumpir
        la ejecución.
\end{itemize}

\section{Ingreso a la plataforma Moodle}
\label{sec:ingreso-moodle}

\begin{stepbox}{1}
  Abrir el navegador e ingresar la dirección URL de la plataforma \moodle
  proporcionada por el profesor o institución.
\end{stepbox}

\begin{stepbox}{2}
  En la página de inicio de sesión, introducir el \textbf{nombre de usuario}
  y la \textbf{contraseña} y hacer clic en \menu{Iniciar sesión}.
\end{stepbox}


\section{Localizar y abrir la actividad VPL}
\label{sec:abrir-vpl}

Una vez dentro de \moodle, la sección \menu{My courses} muestra los cursos
en los que el alumno está inscrito.

\begin{stepbox}{1}
  Hacer clic en \menu{My courses} en la barra de navegación superior para
  ver el listado de cursos disponibles.
\end{stepbox}

\begin{figure}[H]
\centering
\includegraphics[width=1\textwidth]{alumno/vista_curso_almno_1.png}
\caption{Vista de cursos disponibles en Moodle para el alumno}
\label{fig:vista-cursos-alumno}
\end{figure}

\begin{stepbox}{2}
  Hacer clic sobre la tarjeta del curso correspondiente para acceder a él.
\end{stepbox}

\begin{stepbox}{3}
  Dentro del curso, localizar la actividad \vpl en la lista de actividades
  y hacer clic sobre ella para abrir su página.
\end{stepbox}

\begin{figure}[H]
\centering
\includegraphics[width=1\textwidth]{alumno/vista_actividad_2.png}
\caption{Página de la actividad VPL con información de entrega}
\label{fig:vista-actividad}
\end{figure}

En la página de la actividad (Figura~\ref{fig:vista-actividad}) se muestra
información relevante:

\begin{itemize}
  \item \textbf{Due date:} fecha y hora límite de entrega.
  \item \textbf{Maximum number of files:} número máximo de archivos
        permitidos (generalmente el archivo \vhdl fuente y su testbench).
  \item \textbf{Type of work:} modalidad de la actividad (individual o grupal).
\end{itemize}

Para ingresar al editor y comenzar a trabajar, hacer clic en el botón
\icono{editar.png} en la parte inferior de la página.

% ── Capítulo 2 ──────────────────────────────────────────────
\chapter{Entrar al Editor de VPL}
\label{ch:editor}

\section{Descripción del entorno de edición}
\label{sec:entorno}

Al acceder al editor de \vpl se carga un entorno compuesto por tres áreas
principales:

\begin{itemize}
  \item \textbf{Barra de herramientas:} en la parte superior contiene las
        acciones disponibles: crear archivo, guardar, ejecutar (\textit{Run}),
        depurar (\textit{Debug}), generar testbench y evaluar, entre otras.
  \item \textbf{Panel de archivos y editor:} área central donde se escribe y
        edita el código \vhdl. Las pestañas superiores muestran los archivos
        abiertos y el panel \textit{File list} los lista a la izquierda.
  \item \textbf{Panel de resultados (\textit{Compilation}):} aparece a la
        derecha al ejecutar cualquier acción y muestra la salida del
        compilador o del simulador.
\end{itemize}

Para crear el primer archivo de trabajo, hacer clic en el ícono
\accion{crear_archivo.png}{Crear nuevo archivo} (primer ícono de la barra de herramientas).
Aparece un cuadro de diálogo solicitando el nombre del nuevo archivo
(Figura~\ref{fig:acceso-editor}). Al scceder por primera vez al editor, en automatico lanza el recuadro para solicitar
el nombre del nuevo archivo, por lo que el paso de hacer clic en el ícono de crear nuevo archivo se puede omitir si es la primera vez que se accede al editor.

\begin{figure}[H]
\centering
\includegraphics[width=1\textwidth]{alumno/acceso_editor_vpl_3.png}
\caption{Editor de VPL con el diálogo para crear un nuevo archivo}
\label{fig:acceso-editor}
\end{figure}

\section{Ajustes del editor}
\label{sec:ajustes-editor}

El editor incluye resaltado de sintaxis para \vhdl, indentación automática
y deshacer/rehacer cambios con los controles \icono{deshacer_rehacer.png} de la
barra de herramientas.

\begin{tipbox}[Pantalla completa]
  El ícono de pantalla completa \icono{expandir_editor.png} (extremo derecho de
  la barra de herramientas)
  amplía el editor para aprovechar todo el espacio disponible, lo que
  resulta útil al revisar el código y los resultados al mismo tiempo.
\end{tipbox}

% ── Capítulo 3 ──────────────────────────────────────────────
\chapter{Crear Archivos VHDL}
\label{ch:crear-vhdl}

\section{Estructura básica de un archivo VHDL}
\label{sec:estructura-vhdl}

Un archivo \vhdl mínimo consta de tres partes:

\begin{enumerate}
  \item \textbf{Declaración de bibliotecas} --- importación de paquetes
        estándar necesarios (por ejemplo \texttt{ieee.std\_logic\_1164}).
  \item \textbf{Entidad (\textit{entity})} --- define el nombre del
        componente y sus puertos de entrada y salida.
  \item \textbf{Arquitectura (\textit{architecture})} --- describe el
        comportamiento o la estructura interna del componente.
\end{enumerate}


\section{Crear un nuevo archivo en VPL}
\label{sec:nuevo-archivo}

\begin{stepbox}{1}
  En la barra de herramientas, hacer clic en el ícono
  \accion{crear_archivo.png}{Crear nuevo archivo}.
\end{stepbox}

\begin{stepbox}{2}
  En el cuadro de diálogo, escribir el nombre del archivo incluyendo la
  extensión \texttt{.vhd} (por ejemplo: \texttt{alu.vhd}) y hacer clic
  en \menu{OK}.
\end{stepbox}

\begin{stepbox}{3}
  El archivo recién creado aparece como una nueva pestaña en el editor y
  queda listado en el panel \textit{File list} (Figura~\ref{fig:fichero-creado}).
\end{stepbox}

\begin{figure}[H]
\centering
\includegraphics[width=1\textwidth]{alumno/fichero_creado_4.png}
\caption{Archivo \texttt{alu.vhd} creado y visible en el editor de VPL}
\label{fig:fichero-creado}
\end{figure}

\begin{stepbox}{4}
  Escribir el código \vhdl en el área del editor. La
  Figura~\ref{fig:codigo-creado} muestra un ejemplo de diseño de una
  Unidad Lógico-Aritmética (\textsc{ALU}) con su entidad y arquitectura
  completas.
\end{stepbox}

\begin{figure}[H]
\centering
\includegraphics[width=1\textwidth]{alumno/codigo_creado_5.png}
\caption{Código VHDL escrito en el archivo \texttt{alu.vhd}}
\label{fig:codigo-creado}
\end{figure}


% ── Capítulo 4 ──────────────────────────────────────────────
\chapter{Compilar Archivos VHDL}
\label{ch:compilar}

\section{Proceso de compilación en VPL}
\label{sec:proceso-compilacion}

La acción \textbf{Run} compila el diseño \vhdl y muestra el resultado en
el panel \textit{Compilation}. Para ejecutarla:

\begin{stepbox}{1}
  Con el archivo \vhdl abierto en el editor, hacer clic en el ícono
  \accion{run_compilar.png}{Run} (ícono de cohete) en la barra de herramientas.
\end{stepbox}

\begin{stepbox}{2}
  Esperar a que el servidor procese la compilación. El panel de resultados
  muestra el progreso con mensajes \texttt{SUCCESS:=>} para cada etapa
  completada correctamente (Figura~\ref{fig:accion-run}).
\end{stepbox}

\begin{figure}[H]
\centering
\includegraphics[width=1\textwidth]{alumno/accion_run_6.png}
\caption{Resultado de compilación exitosa en el panel de resultados}
\label{fig:accion-run}
\end{figure}

Para visualizar el código y los resultados mejor, activar la
vista en pantalla completa \icono{expandir_editor.png} desde la barra de herramientas
(Figura~\ref{fig:full-screen}).

\begin{figure}[H]
\centering
\includegraphics[width=1\textwidth]{alumno/full_screen_7.png}
\caption{Vista en pantalla completa del editor con compilación exitosa}
\label{fig:full-screen}
\end{figure}

\section{Interpretación de errores y advertencias}
\label{sec:errores-compilacion}

Si el código contiene errores, el panel los reporta con el prefijo
\texttt{ERROR:=>} seguido de la descripción del problema y la línea donde
se encontró (Figura~\ref{fig:run-errores}).

\begin{figure}[H]
\centering
\includegraphics[width=1\textwidth]{alumno/accion_run_errores_8.png}
\caption{Panel de resultados mostrando errores de compilación}
\label{fig:run-errores}
\end{figure}

Los mensajes de error de GHDL siguen el formato:

\begin{center}
  \texttt{archivo.vhd:<línea>:<columna>: error: <descripción>}
\end{center}

Para corregir un error, localizar la línea indicada en el mensaje, revisar
la sintaxis en esa sección y ejecutar \accion{run_compilar.png}{Run} nuevamente para
verificar la corrección.


% ── Capítulo 5 ──────────────────────────────────────────────
\chapter{Generación de Código Testbench}
\label{ch:testbench-alumno}

\section{¿Qué es un testbench?}
\label{sec:que-testbench}

Un \textit{testbench} es un archivo \vhdl especial cuyo propósito es
instanciar el componente bajo prueba (\textit{Unit Under Test},
\textsc{UUT}) y aplicarle estímulos (vectores de prueba) para verificar
su comportamiento. El testbench no sintetiza en hardware; solo se utiliza
en simulación.

\section{Generación automática en el entorno VPL}
\label{sec:gen-automatica-tb}

\vpl genera automáticamente un testbench básico a partir del archivo \vhdl
fuente. Para utilizarlo:

\begin{stepbox}{1}
  Asegurarse de que el archivo \vhdl fuente contiene código válido y
  compila sin errores.
\end{stepbox}

\begin{warnbox}[Archivo vacío]
  Si se intenta generar el testbench con el archivo fuente vacío, \vpl
  muestra el error \textit{``The file `\texttt{archivo.vhd}' is empty.
  Add VHDL content before generating a testbench.''}, como se observa
  en la Figura~\ref{fig:tb-error-vacio}. Es necesario escribir al menos
  la entidad y la arquitectura antes de generar el testbench.
\end{warnbox}

\begin{figure}[H]
\centering
\includegraphics[width=1\textwidth]{alumno/accion_generar_tesbench_archivo_vhdl_sin_codigo_10.png}
\caption{Error al intentar generar testbench desde un archivo VHDL vacío}
\label{fig:tb-error-vacio}
\end{figure}

\begin{stepbox}{2}
  Hacer clic en el ícono \accion{generar_testbench.png}{Generate testbench} en la barra de
  herramientas del editor.
\end{stepbox}

\begin{stepbox}{3}
  \vpl analiza la entidad del archivo fuente, extrae sus puertos y genera
  automáticamente un archivo \texttt{<nombre>\_tb.vhd} con la estructura
  básica: declaración de señales, instanciación del componente
  (\textit{port map}) y un proceso \texttt{stim\_proc} con el marcador
  \texttt{-- TODO: add test vectors here}
  (Figura~\ref{fig:testbench-generado}).
\end{stepbox}

\begin{figure}[H]
\centering
\includegraphics[width=1\textwidth]{alumno/accion_generar_testbench_9.png}
\caption{Testbench generado automáticamente para el diseño VHDL}
\label{fig:testbench-generado}
\end{figure}

\section{Personalización del testbench generado}
\label{sec:personalizar-tb}

El testbench generado incluye la estructura necesaria, pero los vectores
de prueba deben añadirse manualmente dentro del proceso \texttt{stim\_proc}.
Cada vector consiste en asignar valores a las señales de entrada y esperar
un tiempo antes del siguiente estímulo:

\begin{lstlisting}[caption={Ejemplo de vectores de prueba en el testbench},
                   label={lst:tv-ejemplo}, language=VHDL]
stim_proc: process is
begin
  -- Vector 1
  A    <= "10101010";
  B    <= "11110001";
  Co   <= '0';
  aluP <= "0001";
  wait for 20 ns;

  -- Vector 2
  A    <= "00001111";
  B    <= "00000001";
  Co   <= '1';
  aluP <= "0001";
  wait for 20 ns;

  wait;
end process stim_proc;
\end{lstlisting}

La Figura~\ref{fig:tb-casos} muestra el testbench con varios vectores de
prueba ya añadidos en el proceso \texttt{stim\_proc}.

\begin{figure}[H]
\centering
\includegraphics[width=1\textwidth]{alumno/casos_de_prueba_en_tesbench_11.png}
\caption{Testbench con vectores de prueba añadidos manualmente}
\label{fig:tb-casos}
\end{figure}

% ── Capítulo 6 ──────────────────────────────────────────────
\chapter{Uso de la Simulación en VPL}
\label{ch:simulacion}

\section{Iniciar la simulación}
\label{sec:iniciar-sim}

La acción \textbf{Debug} compila el diseño junto con su testbench y abre
la herramienta \textbf{GTKWave} para visualizar las formas de onda.
Antes de iniciarla, verificar que:

\begin{itemize}
  \item El archivo fuente \vhdl compile sin errores.
  \item El testbench \texttt{\_tb.vhd} esté creado y contenga vectores
        de prueba con sentencias \texttt{wait}.
\end{itemize}

\begin{stepbox}{1}
  Hacer clic en el ícono \accion{debug_simular.png}{Debug} en la barra de herramientas del
  editor.
\end{stepbox}

\begin{stepbox}{2}
  \vpl compila ambos archivos, genera el archivo de simulación
  (\texttt{.ghw}) y lanza GTKWave. El panel \textit{Compilation} muestra
  el progreso detallado con mensajes \texttt{SUCCESS:=>} e \texttt{INFO:=>}
  sobre la clasificación de archivos, detección de la entidad principal,
  tiempo de simulación y proyecto GTKWave generado
  (Figura~\ref{fig:output-debug}).
\end{stepbox}

\begin{figure}[H]
\centering
\includegraphics[width=1\textwidth]{alumno/output_debug_consola_17.png}
\caption{Salida detallada del proceso de compilación al ejecutar Debug}
\label{fig:output-debug}
\end{figure}

\begin{stepbox}{3}
  Una vez completado el proceso, GTKWave se abre mostrando el árbol de
  señales (\textit{SST}) a la izquierda y el área de formas de onda a la
  derecha (Figura~\ref{fig:gtkwave-vacio}). El área de formas de onda
  aparece inicialmente vacía hasta que se añadan señales.
\end{stepbox}

\begin{figure}[H]
\centering
\includegraphics[width=1\textwidth]{alumno/accion_debug_12.png}
\caption{GTKWave abierto con la jerarquía de señales y área de visualización vacía}
\label{fig:gtkwave-vacio}
\end{figure}

\section{Visualización de formas de onda}
\label{sec:formas-onda}

\begin{stepbox}{1}
  En el panel \textsc{SST} (izquierda), expandir la jerarquía haciendo
  clic sobre los nodos hasta llegar a las señales del circuito bajo prueba
  (Figura~\ref{fig:senales-sim}).
\end{stepbox}

\begin{figure}[H]
\centering
\includegraphics[width=1\textwidth]{alumno/senales_ensimulacion_13.png}
\caption{Árbol de señales SST expandido con menú contextual para importar señales}
\label{fig:senales-sim}
\end{figure}

\begin{stepbox}{2}
  Hacer clic derecho sobre el componente deseado y seleccionar
  \menu{Recurse Import} \textrightarrow{} \menu{Append} para añadir todas
  sus señales al área de visualización.
\end{stepbox}

\begin{stepbox}{3}
  Si GTKWave muestra el cuadro de diálogo \textit{``Really import N
  facilities?''} (Figura~\ref{fig:recurse-warning}), hacer clic en
  \menu{Yes} para confirmar la importación.
\end{stepbox}

\begin{figure}[H]
\centering
\includegraphics[width=1\textwidth]{alumno/preguntar_anadir_senales_14.png}
\caption{Diálogo de confirmación para importar todas las señales del componente}
\label{fig:recurse-warning}
\end{figure}

\begin{stepbox}{4}
  Las señales aparecen en el panel izquierdo con sus formas de onda en el
  área de visualización (Figura~\ref{fig:visualizacion-senales}).
\end{stepbox}

\begin{figure}[H]
\centering
\includegraphics[width=1\textwidth]{alumno/visualizacion_senales_15.png}
\caption{Formas de onda de las señales del diseño visualizadas en GTKWave}
\label{fig:visualizacion-senales}
\end{figure}

Las señales de tipo vector (por ejemplo \texttt{a[7:0]}, \texttt{res[7:0]})
se muestran por defecto como bus. Para visualizar cada bit individualmente,
hacer clic en la flecha de expansión junto al nombre de la señal
(Figura~\ref{fig:desplegar-vectores}).

\begin{figure}[H]
\centering
\includegraphics[width=1\textwidth]{alumno/desplegar_senaels_vectores_16.png}
\caption{Señales vectoriales expandidas mostrando cada bit individualmente en GTKWave}
\label{fig:desplegar-vectores}
\end{figure}

\section{Depuración mediante simulación}
\label{sec:debug-sim}

GTKWave permite inspeccionar el valor de cada señal en cualquier instante
haciendo clic sobre el área de formas de onda. El valor aparece en la
columna junto al nombre de la señal. Para navegar entre eventos, usar los
controles de avance y retroceso de la barra de herramientas de GTKWave.

Para controlar el tiempo total de simulación, añadir la directiva
\texttt{stine\_stop} como comentario en las primeras líneas del
testbench \texttt{\_tb.vhd}:

\begin{lstlisting}[language=VHDL, numbers=none,
  caption={Directiva para establecer el tiempo de simulación},
  label={lst:stop-time}]
-- stime_stop = 2 ms
\end{lstlisting}

La Figura~\ref{fig:stop-time-tb} muestra el testbench con esta directiva
y la salida del compilador confirmando que el tiempo fue detectado
correctamente.

\begin{figure}[H]
\centering
\includegraphics[width=1\textwidth]{alumno/uso_stime_stop_en_tesbench_18.png}
\caption{Directiva \texttt{stime\_stop} declarada en el testbench y confirmada por el compilador}
\label{fig:stop-time-tb}
\end{figure}

Al lanzar la simulación con esta directiva, GTKWave ajusta el eje temporal
hasta el valor especificado (Figura~\ref{fig:stop-time-sim}), lo que
permite visualizar con mayor resolución el comportamiento del circuito
durante el intervalo de interés.

\begin{figure}[H]
\centering
\includegraphics[width=1\textwidth]{alumno/uso_do_stime_stop_simulacion_19.png}
\caption{GTKWave con el eje temporal ajustado al tiempo definido por \texttt{vpl\_stop\_time}}
\label{fig:stop-time-sim}
\end{figure}

% ── Capítulo 7 ──────────────────────────────────────────────
\chapter{Uso de la Evaluación Automática}
\label{ch:evaluacion-alumno}

\section{¿Cómo funciona la evaluación automática?}
\label{sec:como-evalua}

\begin{warnbox}[Opcional]
 Esta opción por default esta deshabilitada para el alumno, el profesor puede habilitarla o no dependiendo de la configuración de la actividad. Si la evaluación automática no está habilitada, el alumno tendra que esperar a que el profesor evalue su actividad para poder ver los resultados y la calificación obtenida.
\end{warnbox}
Al enviar el diseño para evaluación, \vpl ejecuta el archivo \vhdl del
alumno contra los casos de prueba definidos por el profesor. Para cada
caso, el evaluador:

\begin{enumerate}
  \item Compila el diseño del alumno con GHDL.
  \item Genera un testbench automático a partir de los casos de prueba.
  \item Simula el diseño y compara las salidas obtenidas con las esperadas.
  \item Calcula la calificación final aplicando las penalizaciones definidas
        para cada caso fallido.
\end{enumerate}

El resultado aparece en el panel de comentarios con el detalle de qué
casos pasaron (\texttt{PASS}) y cuáles fallaron (\texttt{FAIL}), junto
con la calificación final.

\section{Enviar el diseño para evaluación}
\label{sec:enviar-evaluacion}

\begin{stepbox}{1}
  Verificar que el diseño compile sin errores utilizando la acción
  \accion{run_compilar.png}{Run} antes de enviarlo.
\end{stepbox}

\begin{stepbox}{2}
  Hacer clic en el ícono \accion{evaluar.png}{Evaluate} en la barra de herramientas del
  editor. \vpl compila el diseño, ejecuta los casos de prueba y muestra el
  resultado en el panel derecho. Como se observa en la
  Figura~\ref{fig:evaluar-alumno}, el panel presenta la calificación
  propuesta (\texttt{Proposed grade}) junto con el reporte de compilación,
  mientras la lista de archivos del proyecto (diseño, \textit{testbench} y
  archivos de soporte) permanece visible a la izquierda.
\end{stepbox}

\begin{figure}[H]
\centering
\includegraphics[width=1\textwidth]{alumno/EVALUAR_POR_ALUMNO.png}
\caption{Vista del editor \vpl al evaluar: lista de archivos, código del
diseño y panel con la calificación propuesta y el reporte de compilación}
\label{fig:evaluar-alumno}
\end{figure}

\begin{stepbox}{3}
  Una vez evaluado, el diseño queda registrado como entrega en \moodle.
  La Figura~\ref{fig:evaluacion-propuesta} muestra la vista de entrega con
  la fecha y hora de envío, la evaluación automática y la calificación
  propuesta sobre 100 puntos. En el reporte se distinguen los mensajes
  \texttt{SUCCESS} de cada etapa (búsqueda de archivos, compilación con
  GHDL y simulación), el conteo de casos superados (\texttt{Casos: 3/3}),
  la nota final (\texttt{Nota: 100/100}) y el comentario
  \textit{Evaluación VHDL Automática}.
\end{stepbox}

\begin{figure}[H]
\centering
\includegraphics[width=1\textwidth]{alumno/EVALUACION_PROPUESTA.png}
\caption{Vista de entrega en \moodle con la evaluación automática: fecha de
envío, calificación propuesta, reporte de compilación y resultado de los
casos de prueba}
\label{fig:evaluacion-propuesta}
\end{figure}

\section{Interpretar la retroalimentación}
\label{sec:retroalimentacion}

El reporte de evaluación indica:

\begin{itemize}
  \item \textbf{PASS:} el caso de prueba fue superado correctamente.
  \item \textbf{FAIL:} la salida obtenida no coincide con la esperada
        en ese caso.
  \item \textbf{Nota:} calificación final calculada sobre 100 puntos.
  \item \textbf{Evaluation output:} resumen con el número de casos
        totales, correctos y fallidos.
\end{itemize}

Leer los mensajes de \texttt{FAIL} para identificar qué señales
presentaron valores incorrectos y en qué momento de la simulación
ocurrieron, lo que orienta la corrección del diseño.

\section{Reintentos y límite de envíos}
\label{sec:reintentos}

El número máximo de entregas está determinado por la configuración del
profesor. Se recomienda:

\begin{itemize}
  \item Usar \accion{run_compilar.png}{Run} para verificar la compilación antes de cada envío.
  \item Depurar el comportamiento del diseño con \accion{debug_simular.png}{Debug} antes de
        gastar un intento de evaluación.
  \item Revisar los mensajes de retroalimentación de intentos anteriores
        para corregir los casos que fallaron.
\end{itemize}

\begin{warnbox}[Límite de intentos]
  Si la actividad tiene un número limitado de evaluaciones, cada clic en
  \accion{evaluar.png}{Evaluate} consume un intento sin importar el resultado obtenido.
  Asegúrese de haber depurado el diseño antes de evaluar.
\end{warnbox}


% ── Capítulo: Conexión y transferencia con servidores remotos ──
\chapter{Conexión y Transferencia con Servidores Remotos}
\label{ch:servidores-remotos}

Además de las acciones estándar de \vpl, el entorno incorpora dos botones
propios para interactuar con servidores externos desde el laboratorio
virtual: uno para \textbf{copiar los archivos del alumno hacia un servidor
remoto} y abrir una sesión \ssh, y otro para \textbf{descargar archivos
desde un servidor remoto} e incorporarlos como archivos del proyecto. Estas
funcionalidades fueron añadidas para conectarse con el generador de archivos
\textit{bitstream} y con el laboratorio remoto de FPGA's.

\section{Conexión al laboratorio remoto (copiar archivos y abrir SSH)}
\label{sec:conexion-lab-remoto}

Esta funcionalidad se añadió para copiar, mediante el comando \texttt{scp},
los archivos del laboratorio virtual hacia un servidor remoto y, a
continuación, establecer una conexión \ssh con dicho servidor. Resulta de
gran utilidad para conectarse al \textbf{generador de archivos bitstream} y
al \textbf{laboratorio remoto de FPGA's}.

\begin{stepbox}{1}
  Hacer clic en el botón de conexión al laboratorio remoto \icono{lab_remoto.png}
  de la barra de
  herramientas del editor. Se abre el modal \textit{Remote Lab}, que
  solicita los datos de acceso al servidor: \texttt{Server IP / Host},
  \texttt{Username} y \texttt{Password}
  (Figura~\ref{fig:remoto-ejecutar}).
\end{stepbox}

\begin{figure}[H]
\centering
\includegraphics[width=1\textwidth]{boton_conexion_lab_remoto/ejecutarr_boton.png}
\caption{Modal \textit{Remote Lab} con los campos de conexión vacíos al
ejecutar el botón}
\label{fig:remoto-ejecutar}
\end{figure}

\begin{stepbox}{2}
  Ingresar las credenciales del servidor: la dirección IP o host (por
  ejemplo \texttt{148.204.59.117}), el nombre de usuario (\texttt{alumno01})
  y la contraseña. Pulsar \menu{Connect} para iniciar la transferencia y la
  conexión (Figura~\ref{fig:remoto-credenciales}).
\end{stepbox}

\begin{figure}[H]
\centering
\includegraphics[width=1\textwidth]{boton_conexion_lab_remoto/ingresar_credenciales.png}
\caption{Ingreso de IP, usuario y contraseña en el modal \textit{Remote Lab}}
\label{fig:remoto-credenciales}
\end{figure}

\begin{stepbox}{3}
  \vpl copia los archivos del alumno al servidor con \texttt{scp} y abre la
  sesión \ssh. La consola muestra el progreso: verificación de la conexión
  al puerto \texttt{22}, el mensaje \texttt{Transfiriendo 2 archivo(s)
  VHDL...} seguido de \texttt{Transferencia completada} y, finalmente, el
  prompt interactivo del servidor (\texttt{alumno01@jetrevin:\textasciitilde\ \$}), donde
  el comando \texttt{ls} confirma que los archivos \texttt{alu.vhd} y
  \texttt{alu\_tb.vhd} ya están en el servidor
  (Figura~\ref{fig:remoto-conexion}).
\end{stepbox}

\begin{figure}[H]
\centering
\includegraphics[width=1\textwidth]{boton_conexion_lab_remoto/conexion_y_copiado_de_arcivos.png}
\caption{Consola \ssh tras copiar los archivos del alumno al servidor:
transferencia completada y sesión interactiva establecida}
\label{fig:remoto-conexion}
\end{figure}

\section{Descargar archivos desde un servidor remoto}
\label{sec:descargar-archivos-remoto}

Como su nombre lo indica, esta funcionalidad sirve para descargar archivos
de un servidor remoto y mostrarlos como archivos del alumno dentro del
proyecto. Es de utilidad para obtener los archivos de configuración
\texttt{.bit} (bitstream) y los archivos de restricciones \texttt{.xdc}.

\begin{stepbox}{1}
  Hacer clic en el botón de descarga \icono{descargar_archivos.png}. Se abre el
  modal \textit{Download from
  Remote}, con los campos \texttt{Server IP / Host}, \texttt{Username},
  \texttt{Password} y el campo \texttt{Remote file(s)} para indicar qué
  archivos descargar (Figura~\ref{fig:descargar-modal}).
\end{stepbox}

\begin{figure}[H]
\centering
\includegraphics[width=1\textwidth]{boton_descargar_Archivos/modal_boton.png}
\caption{Modal \textit{Download from Remote} con los campos de conexión y de
selección de archivos}
\label{fig:descargar-modal}
\end{figure}

Para indicar qué archivos descargar, el campo \texttt{Remote file(s)} acepta
los siguientes formatos:

\begin{itemize}
  \item \textbf{Nombre de archivo:} un único archivo, por ejemplo
        \texttt{archivo.bit}.
  \item \textbf{Nombres separados por coma:} varios archivos, por ejemplo
        \texttt{archivo1.bit,archivo2.xdc}.
  \item \textbf{Ruta específica:} un archivo dentro de una ruta, por ejemplo
        \texttt{ruta/archivo.bit}.
  \item \textbf{Múltiples archivos en una ruta:} usando la sintaxis
        \texttt{ruta/archivos:archivo1.bit,archivo2.xdc}.
\end{itemize}

\begin{stepbox}{2}
  Ingresar las credenciales del servidor y, en el campo \texttt{Remote
  file(s)}, los archivos a descargar (por ejemplo
  \texttt{fichero1.txt,fichero2.vhd}). Pulsar \menu{Download}
  (Figura~\ref{fig:descargar-credenciales}).
\end{stepbox}

\begin{figure}[H]
\centering
\includegraphics[width=1\textwidth]{boton_descargar_Archivos/ingresar_credenciales_y archivos.png}
\caption{Ingreso de credenciales y de la lista de archivos a descargar en el
modal \textit{Download from Remote}}
\label{fig:descargar-credenciales}
\end{figure}

\begin{stepbox}{3}
  \vpl se conecta al servidor y localiza los archivos solicitados. La consola
  muestra la sesión \ssh y, con \texttt{ls}, los archivos disponibles en el
  servidor remoto (\texttt{fichero.txt} y \texttt{fichero2.vhd})
  (Figura~\ref{fig:descargar-servidor}).
\end{stepbox}

\begin{figure}[H]
\centering
\includegraphics[width=1\textwidth]{boton_descargar_Archivos/ver_archivos_en_servidor.png}
\caption{Verificación en consola de los archivos disponibles en el servidor
remoto antes de la descarga}
\label{fig:descargar-servidor}
\end{figure}

\begin{stepbox}{4}
  Al completarse la descarga, los archivos se incorporan a la lista de
  archivos del proyecto y quedan visibles como archivos del alumno
  (\texttt{fichero.txt} y \texttt{fichero2.vhd} aparecen en el explorador de
  la izquierda), listos para usarse en la actividad
  (Figura~\ref{fig:descargar-archivos}).
\end{stepbox}

\begin{figure}[H]
\centering
\includegraphics[width=1\textwidth]{boton_descargar_Archivos/archivos_descargados.png}
\caption{Archivos descargados desde el servidor remoto incorporados a la
lista de archivos del alumno}
\label{fig:descargar-archivos}
\end{figure}

\begin{warnbox}[Límite de archivos]
  La descarga está limitada por el número máximo de archivos que puede
  entregar el alumno, valor definido por el profesor en la configuración de
  la actividad. Si la descarga supera ese límite, los archivos no podrán
  incorporarse al proyecto.
\end{warnbox}


%           PARTE II — GUÍA DEL PROFESOR

\part{Guía del Profesor}


% ── Capítulo 8 ──────────────────────────────────────────────
\chapter{Creación de Actividad VPL}
\label{ch:crear-actividad}

\section{Precondiciones}
\label{sec:precondiciones-profesor}

Antes de crear una actividad \vpl, verificar que se cumplan los siguientes requisitos:

\begin{itemize}
  \item \textbf{Cuenta con rol de profesor y permisos de edición:} el usuario debe tener asignado el rol de \textit{Profesor} en \moodle con capacidad de editar el curso. Sin este rol, las opciones de configuración de actividades no estarán disponibles.
  \item \textbf{Sesión activa:} el profesor debe haber iniciado sesión en la plataforma \moodle antes de realizar cualquier operación.
  \item \textbf{Curso creado:} debe existir al menos un curso en \moodle al cual el profesor tenga acceso de edición. La actividad \vpl se agrega dentro de un curso existente.
  \item \textbf{Plugin de VPL instalado:} El plugin de VPL debe estar instalado y activo en la plataforma \moodle.
\end{itemize}



\section{Crear una actividad VPL}
\label{sec:crear-actividad-vpl}
Para crear una actividad \vpl en un curso de \moodle, seguir estos pasos: 
\begin{stepbox}{1}
  Ingresar al curso donde se desea agregar la actividad \vpl.
\end{stepbox}
\begin{figure}[H]
\centering
\includegraphics[width=1\textwidth]{moodle/mis_cursos_moodle.png}
\caption{Cursos disponibles en Moodle}
\label{fig:mis-cursos-moodle}
\end{figure}
\begin{stepbox}{2}
  Activar el modo de edición del curso haciendo clic en el botón \menu{Activar edición}.
\end{stepbox}
\begin{figure}[H]
\centering
\includegraphics[width=1\textwidth]{moodle/curso_vpl.png}
\caption{Cursos donde se desea agregar la actividad VPL}
\label{fig:curso-vpl}
\end{figure}
\begin{stepbox}{3}
  En la sección del curso donde se desea colocar la actividad, hacer clic en \menu{Añadir una actividad o recurso}.En la ventana emergente, seleccionar \textbf{Virtual Programming Lab (VPL)}.
\end{stepbox}
\begin{figure}[H]
\centering
\includegraphics[width=1\textwidth]{vpl/crear_actividad_vpl.png}
\caption{Crear actividad VPL}
\label{fig:crear-actividad-vpl}
\end{figure}

Una vez creada la actividad VPL, se nos muestra una vista para la configuración principal de la actividad, donde se pueden establecer diferentes configuraciones, de las cuales mencionaremos las mas relevantes para el correcto funcionamiento de la actividad, sin embargo, se recomienda revisar cada una de las opciones para personalizar la actividad a las necesidades del curso.

\subsection{Configuraciones generales}
\label{sec:config-generales}
Incluye el nombre de la actividad (obligatorio), una descripción corta y una descripción detallada donde podemos añadir mas recursos (texto, imagenes, videos, etc.).
\begin{figure}[H]
\centering
\includegraphics[width=1\textwidth]{vpl/actividad_vpl.png}
\caption{Configuraciones generales de la actividad VPL}
\label{fig:config-generales-vpl}
\end{figure}

\subsection{Restricciones de entrega}
\label{subsec:restricciones}
En esta sección se puedes establecer configuraciones como el numero maximo de archivos que el alumno puede entrgar, el tipo de trabajo (individual o grupal), fecha de entrega, entre otras opciones que pueden ser útiles para controlar la entrega de los trabajos por parte de los alumnos.

Aqui lo mas importante es configurar el numero maximo de archivos que el alumno puede entregar, por default se establecieron 4 archivos maximos, considerando un archivo vhdl fuente,su archivo testbench generado, el archivo .bit y el archivo de restricciones .xdc, sin embargo, dependiendo de la actividad se pueden establecer mas archivos maximos para que el alumno pueda entregar archivos adicionales (por ejemplo, un archivo de texto con una explicación del diseño o un diagrama del circuito).
\begin{figure}[H]
\centering
\includegraphics[width=1\textwidth]{vpl/restricciones_actividad_vpl.png}
\caption{Restricciones de entrega en la actividad VPL}
\label{fig:restricciones-vpl}
\end{figure}

\subsection{Otras configuraciones}
\label{subsec:config-otras}
EL VPL es un plugin muy completo que ofrece muchas opciones de configuración, por lo que se recomienda revisar cada una de las secciones para personalizar la actividad a las necesidades del curso, sin embargo, se recomienda revisar la  documentacion propia de VPL for Moodle para  saber mas acerca de las otras configuraciones.


Una vez configurada la actividad, se debe guardar los cambios para que la actividad quede disponible para los alumnos.
\begin{figure}[H]
\centering
\includegraphics[width=1\textwidth]{vpl/guardar_actividad_vpl.png}
\caption{Guardar cambios en la actividad VPL}
\label{fig:guardar-actividad-vpl}
\end{figure}

Una vez guardadas las configuraciones establecidas, la actividad deberia verse como en la siguiente imagen:
\begin{figure}[H]
\centering
\includegraphics[width=1\textwidth]{vpl/overview_actividad_vpl.png}
\caption{Visión general de la actividad VPL}
\label{fig:vista_general}
\end{figure}

% ── Capítulo 9 ──────────────────────────────────────────────
\chapter{Acciones Permitidas}
\label{ch:acciones}
En esta sección se describen como configurar las acciones que ofrece el entorno de VPL, esto respecto a la ejecución, depuración y evaluación de los diseños VHDL entregados por los alumnos.
Es importante configurar esta sección una vez creada la actividad, ya que si visualizamos la imagen \ref{fig:vista_general}, observamos que por se visualiza la acción  RUN habilitada y Evaluate deshabilitada, 
por lo que hay que configurar las  acciones para permitir o no al alumno usarlas.

\section{Tipos de acciones en VPL}
\label{sec:tipos-acciones}
En el laboratorio virtual de VPL, existen 5 tipos principales de acciones  que se pueden configurar para los alumnos:
\begin{itemize}
  \item \textbf{Run (Ejecutar):} Permite a los alumnos ejecutar su código VHDL para verificar su funcionamiento básico. Esta acción generalmente compila y simula el diseño, mostrando los resultados de la simulación.
  \item \textbf{Debug (Depurar):} Permite a los alumnos depurar su código VHDL, lo que puede incluir la visualización de formas de onda, inspección de señales y análisis detallado del comportamiento del diseño durante la simulación.
  \item \textbf{Evaluate (Evaluar):} Permite a los alumnos enviar su diseño para evaluación automática, donde se ejecutan casos de prueba predefinidos para verificar la corrección del diseño y asignar una calificación basada en los resultados. 
  \item \textbf{Testbench (Generar Testbench):} Permite a los alumnos generar automáticamente un testbench básico para su diseño VHDL, lo que facilita la simulación y verificación de su código.
  \item  \textbf{Remote Lab (Laboratorio Remoto):} Permite establecer una conexión ssh y copiar los archivos del alumno al servidor.
  \item  \textbf{Download from Remote  (Descargar remotamente):} Permite descargar archivos de un servidor remoto y mostrarlos en la lista de archivos del alumno..


\end{itemize}

\section{Configurar acciones disponibles para el alumno}
\label{sec:config-acciones}
Para habilitar o deshabilitar estas acciones para los alumnos, se deben seguir los siguientes pasos:
\begin{stepbox}{1}
  Ingresar a la actividad VPL creada y hacer clic en el botón \menu{MAS / More} para que se desplieguen mas opciones del VPL.
\end{stepbox}
\begin{figure}[H]
\centering
\includegraphics[width=1\textwidth]{profesor/mas_actividad_vpl.png}
\caption{Mas configuraciones de la actividad VPL}
\label{fig:mas-actividad-vpl}
\end{figure}
\begin{stepbox}{2}
  En la sección de \textbf{Acciones de ejecución}, seleccionar las acciones que se desean habilitar para los alumnos.
\end{stepbox}
\begin{figure}[H]
\centering
\includegraphics[width=1\textwidth]{profesor/image.png}
\caption{Opciones de ejecución en la actividad VPL}
\label{fig:opciones-ejecucion-vpl}
\end{figure} 

\begin{stepbox}{3}
  Dentro de esta sección lo mas importante es habilitar o deshabilitar las opciones de \textbf{Run} , \textbf{Debug} , \textbf{Remote Lab} , \textbf{Generate Testbench} y \textbf{Evaluate}, ya que estas son las acciones principales para que el alumno pueda ejecutar.
\end{stepbox}
\begin{figure}[H]
\centering
\includegraphics[width=1\textwidth]{profesor/permitir_opciones_vpl.png}
\caption{Permitir opciones de ejecución en la actividad VPL}
\label{fig:permitir_opciones}
\end{figure} 



% ── Capítulo 10 ─────────────────────────────────────────────
\chapter{Configuración de Casos de Prueba}
\label{ch:ficheros}

El archivo \archivo{vpl\_evaluate.cases} contiene los casos de prueba que el
evaluador \archivo{vpl\_evaluate.sh} ejecuta al calificar un diseño \vhdl.
El evaluador compila el código del alumno, detecta la entidad top, extrae sus
puertos, genera un testbench \vhdl automáticamente a partir de los casos, lo
simula con GHDL y calcula la nota final.

\section{Archivo \texttt{vpl\_evaluate.cases}}
\label{sec:vpl-evaluate-cases}

El archivo tiene dos bloques diferenciados:

\begin{enumerate}
  \item \textbf{Configuración global} --- parámetros que aplican a todos los
        casos (reloj, reset, tiempo de simulación). Aparecen antes del primer
        \texttt{case}.
  \item \textbf{Definición de casos} --- uno o más bloques \texttt{case}, cada
        uno con sus secciones \texttt{input:} y \texttt{output:}.
\end{enumerate}

Las líneas que comienzan con \texttt{\#} son comentarios y se ignoran.
Los nombres de parámetro son insensibles a mayúsculas y minúsculas.

\subsection{Sintaxis general de los casos de prueba}
\label{subsec:sintaxis-general}

\subsubsection{Configuración global}

Los parámetros globales se escriben como \texttt{parámetro = valor} y se
colocan al inicio del archivo, antes de cualquier \texttt{case}.
La Tabla~\ref{tab:params-global} lista los parámetros disponibles.

\begin{longtable}{L{3.6cm} L{3.2cm} L{5.7cm}}
  \caption{Parámetros de configuración global}
  \label{tab:params-global}\\
  \toprule
  \textbf{Parámetro} & \textbf{Ejemplo} & \textbf{Descripción} \\
  \midrule
  \endfirsthead
  \toprule
  \textbf{Parámetro} & \textbf{Ejemplo} & \textbf{Descripción} \\
  \midrule
  \endhead
  \bottomrule
  \endfoot
  \texttt{stop\_time\_all} & \texttt{1 ms} &
    Tiempo máximo de simulación global. \\
  \addlinespace
  \texttt{clock} & \texttt{CLK} &
    Nombre del puerto de reloj. Si se omite, el evaluador lo detecta
    automáticamente buscando puertos llamados \texttt{clk}, \texttt{clock}
    o \texttt{ck}. \\
  \addlinespace
  \texttt{clock\_period} & \texttt{10 ns} &
    Período completo del reloj. \\
  \addlinespace
  \texttt{reset} & \texttt{RESET} &
    Nombre del puerto de reset. Si se omite se busca entre
    \texttt{reset}, \texttt{rst}, \texttt{clr} y variantes con sufijo
    \texttt{\_n}. \\
  \addlinespace
  \texttt{reset\_active} & \texttt{1} &
    Nivel activo del reset: \texttt{1} = activo alto, \texttt{0} = activo
    bajo. \\
  \addlinespace
  \texttt{reset\_type} & \texttt{async} &
    Tipo de reset: \texttt{async} o \texttt{sync}. \\
  \addlinespace
  \texttt{reset\_cycles} & \texttt{2} &
    Ciclos de reloj durante los que se mantiene el reset al inicio de la
    simulación. \\
\end{longtable}

\subsubsection{Definición de un caso}

Cada caso comienza con la línea \texttt{case = \textit{descripción}} y contiene
los parámetros opcionales listados en la Tabla~\ref{tab:params-caso}, seguidos
de las secciones de señales.

\begin{longtable}{L{3.6cm} L{3.2cm} L{5.7cm}}
  \caption{Parámetros por caso de prueba}
  \label{tab:params-caso}\\
  \toprule
  \textbf{Parámetro} & \textbf{Ejemplo} & \textbf{Descripción} \\
  \midrule
  \endfirsthead
  \toprule
  \textbf{Parámetro} & \textbf{Ejemplo} & \textbf{Descripción} \\
  \midrule
  \endhead
  \bottomrule
  \endfoot
  \texttt{case} & \texttt{Prueba AND} &
    Nombre del caso; aparece en los reportes de fallo. \\
  \addlinespace
  \texttt{stop\_time} & \texttt{50 ns} &
    Tiempo de simulación específico para este caso.
    Sobreescribe \texttt{stop\_time\_all} solo para este caso. \\
  \addlinespace
  \texttt{grade reduction} & \texttt{25\%} &
    Penalización si el caso falla. Se admite porcentaje (\texttt{\%}) o
    valor absoluto. Si se omite, la penalización se reparte
    equitativamente entre todos los casos. \\
\end{longtable}

\subsubsection{Secciones de señales}

Dentro de cada caso se declaran dos secciones:

\begin{itemize}
  \item \texttt{input:} --- valores que se aplican a los puertos de entrada.
  \item \texttt{output:} --- valores esperados en los puertos de salida.
\end{itemize}

Cada señal se especifica en una línea indentada con el formato:

\begin{center}
  \texttt{~~~~NOMBRE\_SEÑAL = valor}
\end{center}

El valor puede ser:

\begin{itemize}
  \item \textbf{Escalar} --- un único vector de bits: \texttt{A = 1010}.
  \item \textbf{Array} --- varios valores para múltiples ciclos de reloj:
        \texttt{X = [0, 1, 1, 0]}.
  \item \textbf{Comodín} --- guion bajo \texttt{\_} dentro de un array de
        salida indica que ese ciclo no se verifica:
        \texttt{Z = [\_, \_, 1, 0]}.
\end{itemize}

Si alguna señal de entrada o salida usa sintaxis de array, el evaluador
activa el \textbf{modo secuencial}: genera un testbench que aplica un valor
por ciclo de reloj y verifica la salida al final de cada ciclo.
Sin arrays y sin reloj detectado, opera en \textbf{modo combinatorio}:
aplica las entradas, espera \texttt{stop\_time} y verifica las salidas una
sola vez.

\subsection{Sintaxis específica para evaluar código VHDL}
\label{subsec:sintaxis-vhdl}

\subsubsection{Circuitos combinatorios}

Para circuitos sin reloj se omite la configuración global de clock/reset.
El evaluador aplica los valores de \texttt{input:}, espera \texttt{stop\_time}
y compara las salidas declaradas en \texttt{output:}.

\begin{lstlisting}[caption={Caso combinatorio --- sumador de 4 bits con acarreo},
                   label={lst:cases-comb}]
# Tiempo de espera por defecto para todos los casos
stop_time_all = 50 ns

case = Suma basica sin acarreo
stop_time = 20 ns
grade reduction = 50%
input:
    A  = 0011
    B  = 0101
    Co = 0
output:
    S  = 1000
    C4 = 0

case = Suma con acarreo de entrada
stop_time = 20 ns
grade reduction = 50%
input:
    A  = 1111
    B  = 0001
    Co = 1
output:
    S  = 0001
    C4 = 1
\end{lstlisting}

\subsubsection{Circuitos secuenciales}

Para circuitos con reloj se declara la configuración global antes del primer
\texttt{case}. Los valores de entrada y salida se escriben como arrays
\texttt{[v0, v1, \ldots]}, donde cada elemento corresponde a un ciclo de
reloj. El evaluador genera el reloj, aplica el reset durante
\texttt{reset\_cycles} ciclos y luego recorre el array ciclo a ciclo.

\begin{lstlisting}[caption={Caso secuencial --- FSM Mealy},
                   label={lst:cases-seq}]
clock         = CLK
clock_period  = 10 ns
reset         = RESET
reset_active  = 0       # activo bajo
reset_type    = async
reset_cycles  = 2
stop_time_all = 1 ms

# Secuencia X=[0,0,1,1] desde estado inicial C
# Transiciones: C->B->A->B->C
case = Recorrido C-B-A-B-C
grade reduction = 60%
input:
    X = [0, 0, 1, 1]
output:
    Z1 = [1, 0, 1, 1]
    Z2 = [0, 1, 1, 0]

case = Una transicion C a B
grade reduction = 40%
input:
    X = [0]
output:
    Z1 = [1]
    Z2 = [0]
\end{lstlisting}

\begin{warnbox}[Longitud de arrays]
  Los arrays de \texttt{input:} y \texttt{output:} de un mismo caso deben
  tener la misma longitud. El evaluador no rellena valores faltantes.
\end{warnbox}

\subsubsection{Uso del comodín \texttt{\_}}

El comodín \texttt{\_} en un array de salida indica que ese ciclo no se
verifica. Es útil cuando la salida del circuito no está definida durante los
primeros ciclos (por ejemplo, mientras se llena un pipeline o un registro de
desplazamiento).

\begin{lstlisting}[caption={Comodin en salidas de registro de desplazamiento},
                   label={lst:cases-wildcard}]
clock         = CLK
clock_period  = 10 ns
reset         = CLR
reset_active  = 1
reset_type    = async
reset_cycles  = 2
stop_time_all = 1 ms

# Ciclo 1: carga L=1 -> Q=100000000000
# Ciclo 2: desplaza SHE=1 -> Q=010000000000
# Ciclo 3: desplaza SHE=1 -> Q=001000000000
case = Cargar y desplazar
grade reduction = 35%
input:
    L   = [1, 0, 0]
    SHE = [0, 1, 1]
    ES  = [0, 0, 0]
    D   = 100000000000
output:
    LED = [_, 0100010000000000, 0100001000000000]
\end{lstlisting}

\section{Usar el archivo de casos para evaluar diseños VHDL}

Una vez que se ha mencionado la sintaxis general de los casos de prueba, se pueden escribir casos específicos para evaluar diseños VHDL.
Para ello, usaremos el archivo \texttt{vpl\_evaluate.cases} que se encuentra dentro de los archivos de ejecución o en la sección de configuracion de test de la actividad VPL, y se pueden escribir casos específicos para evaluar los diseños VHDL entregados por los alumnos.
\begin{figure}[H]
\centering
\includegraphics[width=1\textwidth]{vpl/test_configuration.png}
\caption{Archivo de configuración de casos de prueba en la actividad VPL}
\label{fig:test_configuration}
\end{figure} 

La edición de este archivo tambien se hace usando el editor de código integrado en VPL, por lo que se pueden usar las mismas herramientas de edición, como el resaltado de sintaxis, la indentación automática y la búsqueda de texto para facilitar la escritura de los casos de prueba.
Una vez escritos los casos de prueba, se deben guardar los cambios para que el evaluador pueda usarlos al calificar los diseños entregados por los alumnos.
\begin{figure}[H]
\centering
\includegraphics[width=1\textwidth]{vpl/test_creado.png}
\caption{Archivo de casos de prueba creado y guardado en la actividad VPL}
\label{fig:test_creado}
\end{figure} 
Una vez teniendo los casos de prueba escritos y guardados, el evaluador los ejecutará automáticamente cada vez que un alumno envíe su diseño para evaluación, generando un reporte con los resultados de cada caso y la calificación final basada en las penalizaciones definidas.
En la seccion \ref{ch:test-profesor} se explica cómo el profesor puede verificar que los casos de prueba funcionan correctamente antes de que los alumnos entreguen sus diseños.

% ── Capítulo 11 ─────────────────────────────────────────────
\chapter{Test de Evaluación por Parte del Profesor}
\label{ch:test-profesor}

VPL permite al profesor verificar que el sistema de evaluación automática funciona correctamente \emph{antes} de que los alumnos entreguen sus soluciones. Para ello, el profesor proporciona soluciones de referencia —correctas e incorrectas— y VPL las evalúa automáticamente, generando un reporte que indica si cada caso obtuvo la calificación esperada.

% ── 11.1 ─────────────────────────────────────────────────────
\section{Propósito}

El test de evaluación sirve para:

\begin{itemize}
  \item Confirmar que los casos de prueba (\texttt{vpl\_evaluate.cases}) detectan
        errores en diseños incorrectos.
  \item Verificar que una solución correcta obtiene la calificación máxima.
  \item Detectar errores de configuración en los scripts de evaluación antes
        de publicar la actividad.
  \item Repetir la verificación cada vez que se actualicen los scripts o se
        cambie el servidor de ejecución.
\end{itemize}

% ── 11.2 ─────────────────────────────────────────────────────
\section{Estructura de la carpeta de pruebas}
\label{sec:test-estructura}

Dentro de los \textbf{Archivos de ejecución} de la actividad se debe crear la
carpeta raíz:

\begin{center}
  \texttt{vpl\_evaluation\_tests/}
\end{center}

Cada subcarpeta dentro de ella representa un caso de prueba y debe seguir
\emph{exactamente} el formato:

\begin{center}
  \texttt{[pass|fail]-\textit{<nombre>}[-\textit{<calificacion>}]}
\end{center}

\begin{itemize}
  \item \texttt{pass} — solución correcta; se espera calificación igual a la
        máxima definida en la actividad (o igual a \textit{<calificacion>} si
        se especifica).
  \item \texttt{fail} — solución incorrecta; se espera calificación
        \emph{menor} que la máxima (o exactamente \textit{<calificacion>} si
        se especifica).
  \item \textit{<nombre>} — descripción breve del escenario, sin guiones.
  \item \textit{<calificacion>} — opcional; calificación numérica esperada
        (entero o decimal).
\end{itemize}

Cada subcarpeta debe contener los archivos fuente del diseño que se desea
evaluar (por ejemplo, el archivo \texttt{.vhd} del circuito).

\subsection{Ejemplos de nombres de subcarpeta}

\begin{center}
\begin{tabular}{L{5.5cm} L{8cm}}
  \toprule
  \textbf{Nombre} & \textbf{Significado} \\
  \midrule
  \texttt{pass-Solucion correcta-10}  & Diseño correcto; se espera nota 10 \\
  \texttt{pass-Correcto}              & Diseño correcto; se espera nota máxima de la actividad \\
  \texttt{fail-Error de salida-3}     & Diseño incorrecto; se espera nota exacta 3 \\
  \texttt{fail-Ciclo infinito-0}      & Diseño con bucle infinito; se espera nota 0 \\
  \texttt{fail-Error de compilacion}  & Diseño que no compila; se espera nota $<$ máxima \\
  \bottomrule
\end{tabular}
\end{center}

\subsection{Estructura de directorios}

La jerarquía de archivos debe quedar así:

\begin{lstlisting}[caption={Estructura mínima de \texttt{vpl\_evaluation\_tests}},
                   label={lst:test-dir}, numbers=none]
vpl_evaluation_tests/
    pass-Solucion correcta-10/
        mi_circuito.vhd
    fail-Error de salida-3/
        mi_circuito.vhd
    fail-Ciclo infinito-0/
        mi_circuito.vhd
    fail-Error de compilacion/
        mi_circuito.vhd
\end{lstlisting}

\begin{notebox}[Requisito mínimo]
  Debe existir \textbf{al menos una subcarpeta} de tipo \texttt{pass}.
  Sin ella, VPL reporta un error de configuración y no ejecuta ninguna prueba.
\end{notebox}

\subsection{Actividades con variaciones}

Si la actividad usa variaciones, se crea una subcarpeta por variación dentro de
\texttt{vpl\_evaluation\_tests}. Cada subcarpeta de variación contiene a su vez
las subcarpetas \texttt{pass-} y \texttt{fail-} correspondientes.

\begin{lstlisting}[caption={Estructura con variaciones},
                   label={lst:test-dir-var}, numbers=none]
vpl_evaluation_tests/
    variacion1/
        pass-Correcto-10/
            mi_circuito.vhd
        fail-Salida erronea-0/
            mi_circuito.vhd
    variacion2/
        pass-Correcto-10/
            mi_circuito.vhd
        fail-Salida erronea-0/
            mi_circuito.vhd
\end{lstlisting}

% ── 11.3 ─────────────────────────────────────────────────────
\section{Pasos para ejecutar el test}
\label{sec:test-pasos}

\begin{stepbox}{1}
  Acceder a la actividad VPL en Moodle y abrir la página
  \textbf{Archivos de ejecución} (menú de administración de la actividad).
\end{stepbox}

\begin{stepbox}{2}
  Crear la carpeta \texttt{vpl\_evaluation\_tests} y las subcarpetas de casos
  de prueba con sus archivos fuente, siguiendo el formato descrito en la
  sección~\ref{sec:test-estructura}.
\end{stepbox}

\begin{stepbox}{3}
  Desde la misma página de \textbf{Archivos de ejecución}, hacer clic en el
  botón \textbf{Evaluar}. VPL ejecutará el proceso de evaluación usando los
  casos definidos en \texttt{vpl\_evaluation\_tests}.
\end{stepbox}

\begin{stepbox}{4}
  Revisar el reporte generado (véase sección~\ref{sec:test-reporte}).
  Si alguna prueba falla, corregir los casos de prueba o los scripts y
  repetir desde el paso~3.
\end{stepbox}

\begin{warnbox}[Seguridad]
  VPL elimina automáticamente la carpeta \texttt{vpl\_evaluation\_tests}
  cuando la actividad se encuentra fuera del modo de evaluación del profesor.
  Esto impide que los alumnos accedan a las soluciones de referencia.
\end{warnbox}

% ── 11.4 ─────────────────────────────────────────────────────
\section{Reporte de evaluación}
\label{sec:test-reporte}

Al ejecutar el test, VPL genera un reporte en el panel de \textbf{Comments} con
tres bloques diferenciados: reporte de compilación, reporte de evaluación resumido
y reporte de evaluación completo (\textit{Full Evaluation Report}).

\subsection{Configuración previa de los casos de prueba}

Antes de ejecutar el test es necesario que el archivo \texttt{vpl\_evaluate.cases}
contenga los casos definidos correctamente. Como se describió en el
capítulo~\ref{ch:ficheros}, este archivo se edita desde la pestaña
\menu{Tests configuration} de la actividad VPL (véase
Figura~\ref{fig:test_configuration}). La Figura~\ref{fig:test_creado} muestra el
archivo con los casos ya escritos y guardados, listos para ser ejecutados por el
evaluador automático.

\subsection{Reporte de compilación}

Indica si el evaluador pudo preparar la solución de referencia para cada caso de
prueba. VPL muestra los pasos realizados: detección del lenguaje de programación,
carga del archivo fuente y verificación del tipo de caso (\texttt{pass} o
\texttt{fail}).

\begin{figure}[H]
\centering
\includegraphics[width=1\textwidth]{vpl/resultado_vpl.png}
\caption{Reporte generado por VPL al evaluar todos los casos de prueba correctamente}
\label{fig:resultado_vpl}
\end{figure}

La Figura~\ref{fig:resultado_vpl} muestra el panel de \textbf{Comments} con los
tres bloques del reporte tras ejecutar la evaluación con una solución correcta.
La sección \textbf{COMPILATION REPORT} confirma que los archivos fuente fueron
compilados sin errores y que la solución de tipo \texttt{pass} con calificación
esperada de~100 fue preparada correctamente.

\subsection{Reporte de evaluación (resumen)}

Muestra para cada caso de \texttt{vpl\_evaluation\_tests} si la calificación
obtenida coincide con la esperada. El formato de cada entrada es:

\begin{center}
  \texttt{<n>) <tipo> (pass|fail type) expected grade mark == <esperada>}\\
  \texttt{Resulted grade mark = <obtenida> and expected grade mark == <esperada>}
\end{center}

Al final aparece la línea \textbf{Final report} que resume el resultado global:

\begin{center}
  \texttt{Final report: \(checkmark\) All <n> tests passed.}
\end{center}

\subsection{Reporte de evaluación completo (\textit{Full Evaluation Report})}

El bloque \textbf{FULL EVALUATION REPORT} detalla la ejecución interna del
evaluador para cada caso. En él se pueden identificar:

\begin{itemize}
  \item \textbf{Compilation output} --- resultado de compilar el diseño con GHDL.
  \item Líneas \texttt{SUCCESS:=>} --- operaciones completadas correctamente:
        búsqueda de archivos, detección de entidad top, extracción de puertos y
        simulación.
  \item Líneas \texttt{INFO:=>} --- configuración detectada: modo combinatorio o
        secuencial, reloj, reset y casos cargados.
  \item Líneas \texttt{PASS:} / \texttt{FAIL:} --- resultado individual de cada
        caso de prueba.
  \item \textbf{Nota final} --- calificación calculada en formato
        \texttt{Nota: <n> / 100.0000}.
  \item \textbf{Evaluation output} --- resumen estadístico: número de casos
        totales, correctos y fallidos.
\end{itemize}

\begin{figure}[H]
\centering
\includegraphics[width=1\textwidth]{vpl/resultado_vpl_output.png}
\caption{Detalle del \textit{Evaluation output} y resumen estadístico al final del reporte}
\label{fig:resultado_vpl_output}
\end{figure}

La Figura~\ref{fig:resultado_vpl_output} muestra el \textit{Evaluation output} al
final del reporte completo, indicando \textbf{casos totales}, \textbf{casos
correctos} y \textbf{casos fallidos}. Esta sección es la que el alumno recibe como
comentario de retroalimentación cuando VPL evalúa su entrega.

\subsection{Reporte con casos fallidos}

Cuando un caso de prueba no produce la calificación esperada, el reporte lo marca
como fallo. La Figura~\ref{fig:resultado_vpl_fail} ilustra un escenario donde el
evaluador detecta un diseño incorrecto: aparece la línea
\texttt{ERROR:=>FAIL: <caso>} en el reporte completo, indicando exactamente qué
caso no pasó. Esto permite al profesor verificar que los casos de prueba detectan
correctamente los errores en los diseños de los alumnos.

\begin{figure}[H]
\centering
\includegraphics[width=1\textwidth]{vpl/resultado_vpl_fail.png}
\caption{Reporte de evaluación con casos fallidos detectados por el evaluador automático}
\label{fig:resultado_vpl_fail}
\end{figure}

\begin{tipbox}[Interpretación de casos \texttt{fail}]
  Un caso de tipo \texttt{fail} en \texttt{vpl\_evaluation\_tests} \emph{pasa} el
  test cuando el evaluador asigna al diseño incorrecto una calificación menor que
  la máxima. Si el reporte muestra \texttt{Final report: All <n> tests passed}
  para un conjunto que incluye casos \texttt{fail}, significa que los casos de
  prueba detectan correctamente los diseños erróneos.
\end{tipbox}

%           PARTE III — GUÍA DEL SERVICIO DE GENERACIÓN DE BITSTREAM

\part{Guía del Servicio de Generación de Bitstream}


% ── Capítulo ────────────────────────────────────────────────
\chapter{Servicio de Generación de Archivos Bitstream}
\label{ch:generador-bitstream}

Esta guía es independiente de los roles de alumno y profesor: documenta el
\textbf{servicio de generación de archivos bitstream}, alojado en un servidor
remoto, que toma un diseño \vhdl, le asigna pines físicos de una tarjeta FPGA
y produce el archivo de configuración \texttt{.bit} junto con su archivo de
restricciones \texttt{.xdc}. El flujo completo combina los botones de
conexión y descarga del editor \vpl con la herramienta de línea de comandos
\texttt{genBIT.sh}.

\section{Conexión al servidor y creación del proyecto}
\label{sec:gb-conexion}

\begin{stepbox}{1}
  Usar el botón de \textbf{conexión al laboratorio remoto} \icono{lab_remoto.png}
  para conectarse al
  servidor de generación de bitstream (IP \texttt{148.204.59.117}). \vpl
  copia con \texttt{scp} los archivos \vhdl del alumno y abre la sesión \ssh.
  La consola muestra \texttt{Transferencia completada} y, con \texttt{ls}, los
  archivos ya presentes en el servidor (\texttt{sumres.vhd} y
  \texttt{sumres\_tb.vhd}), como se ve en la
  Figura~\ref{fig:gb-conexion}.
\end{stepbox}

\begin{figure}[H]
\centering
\includegraphics[width=1\textwidth]{generador_bitstream/1_uso_de_boton_lab_remoto.png}
\caption{Conexión al servidor de generación de bitstream y copia de los
archivos \vhdl mediante el botón de laboratorio remoto}
\label{fig:gb-conexion}
\end{figure}

\begin{stepbox}{2}
  Ejecutar el comando \texttt{genBIT.sh}, que inicia el servicio de generación
  de bitstream. Se muestra el espacio de trabajo del alumno (\textit{FPGA
  Student Workspace}) con la carpeta de proyectos, la lista de proyectos
  existentes y las opciones \texttt{n)} crear, \texttt{d)} eliminar y
  \texttt{q)} salir (Figura~\ref{fig:gb-genbit}).
\end{stepbox}

\begin{figure}[H]
\centering
\includegraphics[width=1\textwidth]{generador_bitstream/2_comando_genBIT.png}
\caption{Menú principal de \texttt{genBIT.sh}: espacio de trabajo y proyectos
existentes}
\label{fig:gb-genbit}
\end{figure}

\begin{stepbox}{3}
  Seleccionar la opción \texttt{n)} para crear un nuevo proyecto (por ejemplo
  \texttt{prueba}). El servicio confirma la creación e indica la ruta donde se
  deben subir los archivos \vhdl:
  \texttt{/home/fpga-students/alumno01/projects/prueba/src}. Conviene fijarse
  en esa ruta, ya que ahí se deben colocar los códigos \vhdl
  (Figura~\ref{fig:gb-crear}).
\end{stepbox}

\begin{figure}[H]
\centering
\includegraphics[width=1\textwidth]{generador_bitstream/3_crear_proyecto.png}
\caption{Creación de un nuevo proyecto y ruta \texttt{src} donde colocar los
archivos \vhdl}
\label{fig:gb-crear}
\end{figure}

\begin{stepbox}{4}
  Salir de \texttt{genBIT.sh} con la opción \texttt{q)} y, desde la sesión
  \ssh, mover el archivo \vhdl que se subió gracias al botón de conexión al
  laboratorio remoto hacia la carpeta del proyecto con \texttt{mv}, por
  ejemplo:
  \texttt{mv sumres.vhd /home/fpga-students/alumno01/projects/prueba/src}
  (Figura~\ref{fig:gb-mover}).
\end{stepbox}

\begin{figure}[H]
\centering
\includegraphics[width=1\textwidth]{generador_bitstream/4_mover_codigo_vhdl_a_proyecto.png}
\caption{Movimiento del archivo \vhdl al directorio \texttt{src} del proyecto
con el comando \texttt{mv}}
\label{fig:gb-mover}
\end{figure}

\section{El editor de proyecto y pines}
\label{sec:gb-editor}

\begin{stepbox}{5}
  Ejecutar nuevamente \texttt{genBIT.sh} e ingresar al proyecto. Se abre la
  interfaz principal \textit{FPGA Project + Pins Editor}
  (Figura~\ref{fig:gb-interfaz}). A la izquierda se muestran los datos del
  proyecto (board \texttt{nexys4ddr-a7-100t}, top \texttt{sumRes4BCA}, archivo
  top, \textit{part}, puertos detectados, modo UI). En el centro está la
  \textbf{vista de configuración del proyecto} con los botones
  \menu{Guardar} y \menu{Continuar a pines}, y la lista de entidades
  detectadas. La barra inferior lista los atajos: \menu{Guardar},
  \menu{Generar XDC}, \menu{Build}, \menu{Actualizar estado}, \menu{Agregar},
  \menu{Modo pines} y \menu{Modo setup}.
\end{stepbox}

\begin{figure}[H]
\centering
\includegraphics[width=1\textwidth]{generador_bitstream/5_ingresar_a_proyecto.png}
\caption{Interfaz principal del generador: vista de configuración del proyecto
y botones de acción}
\label{fig:gb-interfaz}
\end{figure}

\begin{stepbox}{6}
  Hacer clic en \menu{Continuar a pines} para entrar al modo de asignación
  (Figura~\ref{fig:gb-pines}). La interfaz se organiza así:
  \begin{itemize}
    \item \textbf{Lado derecho, arriba:} el \textit{Editor de mapping}, que
          muestra la asignación que se quiere hacer (puerto del diseño y
          recurso de la tarjeta).
    \item Debajo, los \textbf{botones} \menu{Agregar / Actualizar},
          \menu{Eliminar}, \menu{Guardar}, \menu{Generar XDC}, \menu{Build} y
          \menu{Actualizar estado}.
    \item Debajo, la tabla \textbf{Puertos del top} (los puertos de nuestro
          diseño o código \vhdl).
    \item Debajo, la tabla \textbf{Recursos disponibles de la tarjeta}
          (los pines disponibles en la tarjeta).
    \item \textbf{Lado izquierdo, arriba:} la configuración del proyecto.
    \item Debajo, la tabla de \textbf{mapeos} que se han hecho.
    \item Debajo, un recuadro de \textbf{retroalimentación} que describe lo
          que se está haciendo.
  \end{itemize}
\end{stepbox}

\begin{figure}[H]
\centering
\includegraphics[width=1\textwidth]{generador_bitstream/6_interfaz_generador_bitstream.png}
\caption{Modo de asignación de pines: editor de mapping, puertos del diseño y
recursos de la tarjeta}
\label{fig:gb-pines}
\end{figure}

\begin{stepbox}{7}
  Tanto los puertos del diseño como los recursos de la tarjeta se pueden
  \textbf{filtrar o buscar} mediante los campos \texttt{Buscar puerto...} y
  \texttt{Buscar recurso...} (Figura~\ref{fig:gb-filtro}).
\end{stepbox}

\begin{figure}[H]
\centering
\includegraphics[width=1\textwidth]{generador_bitstream/7_uso_filtro.png}
\caption{Uso del filtro para localizar puertos del diseño y recursos de la
tarjeta}
\label{fig:gb-filtro}
\end{figure}

\section{Asignación de pines (mapeo)}
\label{sec:gb-mapeo}

\begin{stepbox}{8}
  Para hacer un mapeo, dar \textbf{doble clic} en el puerto del diseño; este
  se visualiza en el \textit{Editor de mapping} como la asignación que se
  quiere hacer. Hacer lo mismo con el recurso de la tarjeta
  (Figura~\ref{fig:gb-asociar}).
\end{stepbox}

\begin{figure}[H]
\centering
\includegraphics[width=1\textwidth]{generador_bitstream/8_asociar_puertos.png}
\caption{Selección de un puerto del diseño y un recurso de la tarjeta mediante
doble clic}
\label{fig:gb-asociar}
\end{figure}

\begin{stepbox}{9}
  Para agregar y confirmar el mapeo, hacer clic en \menu{Agregar /
  Actualizar}. Del lado izquierdo aparece el mapeo (por ejemplo
  \texttt{A(0) -> SW(0)}) en la tabla de mapeos y, debajo, la descripción de
  lo que se hizo (Figura~\ref{fig:gb-agregar}).
\end{stepbox}

\begin{figure}[H]
\centering
\includegraphics[width=1\textwidth]{generador_bitstream/9_agregar_mapeo.png}
\caption{Mapeo agregado: el puerto del diseño queda asociado a un pin de la
tarjeta}
\label{fig:gb-agregar}
\end{figure}

\begin{stepbox}{10}
  Repetir el procedimiento con todos los puertos hasta asociar todos los
  pines (Figura~\ref{fig:gb-todos}).
\end{stepbox}

\begin{figure}[H]
\centering
\includegraphics[width=1\textwidth]{generador_bitstream/10_todo_mapeados.png}
\caption{Todos los puertos del diseño asociados a pines de la tarjeta}
\label{fig:gb-todos}
\end{figure}

\section{Generación del bitstream}
\label{sec:gb-build}

\begin{stepbox}{11}
  Guardar el mapeo con \menu{Guardar}. Se genera el archivo \texttt{pins.yml}
  y la retroalimentación indica que fue validado y guardado en
  \texttt{.../projects/prueba/pins.yml} (Figura~\ref{fig:gb-guardar}).
\end{stepbox}

\begin{figure}[H]
\centering
\includegraphics[width=1\textwidth]{generador_bitstream/11_guardado_de_mapeo.png}
\caption{Mapeo guardado en el archivo \texttt{pins.yml}}
\label{fig:gb-guardar}
\end{figure}

\begin{stepbox}{12}
  Hacer clic en \menu{Generar XDC} para crear el archivo de restricciones
  \texttt{.xdc}. Este archivo traduce los mapeos realizados a una sintaxis
  entendible para poder usar la FPGA (Figura~\ref{fig:gb-xdc}).
\end{stepbox}

\begin{figure}[H]
\centering
\includegraphics[width=1\textwidth]{generador_bitstream/12_generar_xdc.png}
\caption{Generación del archivo de restricciones \texttt{.xdc} a partir de los
mapeos}
\label{fig:gb-xdc}
\end{figure}

\begin{stepbox}{13}
  Hacer clic en \menu{Build} para enviar el diseño a la cola de procesamiento
  y generar el archivo \texttt{.bit}. El servicio devuelve un \texttt{Job ID}
  y la ruta del proyecto (Figura~\ref{fig:gb-build1}).
\end{stepbox}

\begin{figure}[H]
\centering
\includegraphics[width=1\textwidth]{generador_bitstream/13_construri_bit.png}
\caption{Envío del build a la cola de procesamiento con su \texttt{Job ID}}
\label{fig:gb-build1}
\end{figure}

\begin{stepbox}{14}
  El servicio informa que, para revisar el avance, se debe usar
  \menu{Actualizar estado}, y que al terminar se podrán descargar los
  archivos por SFTP (Figura~\ref{fig:gb-build2}).
\end{stepbox}

\begin{figure}[H]
\centering
\includegraphics[width=1\textwidth]{generador_bitstream/14_construir_bit_parte_2.png}
\caption{Indicaciones del servicio tras enviar el build: seguimiento con
\menu{Actualizar estado}}
\label{fig:gb-build2}
\end{figure}

\begin{stepbox}{15}
  Hacer clic en \menu{Actualizar estado} para saber si el \texttt{.bit} ya se
  construyó. Cuando el proceso finaliza, la retroalimentación muestra
  \texttt{Build terminado} (Figura~\ref{fig:gb-estado}).
\end{stepbox}

\begin{figure}[H]
\centering
\includegraphics[width=1\textwidth]{generador_bitstream/15_actualizar_estado.png}
\caption{Estado del build actualizado: construcción del \texttt{.bit}
terminada}
\label{fig:gb-estado}
\end{figure}

\begin{stepbox}{16}
  Revisar la retroalimentación para ver la ruta donde se generaron los
  archivos de salida (el directorio \texttt{build/} del proyecto), junto con
  los comandos sugeridos para obtener \texttt{prueba.bit} y
  \texttt{xdc\_prueba.xdc} (Figura~\ref{fig:gb-ruta}).
\end{stepbox}

\begin{figure}[H]
\centering
\includegraphics[width=1\textwidth]{generador_bitstream/16_build_terminado_y_ruta_resultados.png}
\caption{Ruta de los archivos de salida generados por el build}
\label{fig:gb-ruta}
\end{figure}

\section{Descarga y visualización de los archivos generados}
\label{sec:gb-descarga}

\begin{stepbox}{17}
  Salir de la interfaz ( Ctrl + q) e ingresar a la ruta de salida para ver qué archivos
  hay. Con \texttt{ls} se listan los resultados del build (entre ellos
  \texttt{prueba.bit} y \texttt{xdc\_prueba.xdc}); copiar esa ruta para la
  descarga (Figura~\ref{fig:gb-archivos}).
\end{stepbox}

\begin{figure}[H]
\centering
\includegraphics[width=1\textwidth]{generador_bitstream/17_visualizacion_de_Archivos_generados.png}
\caption{Listado de los archivos generados en el directorio \texttt{build/}
del proyecto}
\label{fig:gb-archivos}
\end{figure}

\begin{stepbox}{18}
  Usar el botón de \textbf{descargar archivos} \icono{descargar_archivos.png}
  para obtener el \texttt{.bit} y
  el \texttt{.xdc}. En el campo \texttt{Remote file(s)} se indica la ruta del
  build seguida de los archivos a descargar, por ejemplo
  \texttt{ruta/build:prueba.bit,xdc\_prueba.xdc}
  (Figura~\ref{fig:gb-descargar}).
\end{stepbox}

\begin{figure}[H]
\centering
\includegraphics[width=1\textwidth]{generador_bitstream/18_descargar_archivis_bit_y_xdc.png}
\caption{Descarga de los archivos \texttt{.bit} y \texttt{.xdc} mediante el
botón de descarga remota}
\label{fig:gb-descargar}
\end{figure}

\begin{stepbox}{19}
  Visualizar el archivo \texttt{.bit} en el editor de \vpl, ya incorporado a
  la lista de archivos del proyecto (Figura~\ref{fig:gb-ver-bit}).
\end{stepbox}

\begin{figure}[H]
\centering
\includegraphics[width=1\textwidth]{generador_bitstream/19_ve_archivo_bit_editorvpl.png}
\caption{Visualización del archivo \texttt{.bit} descargado en el editor de
\vpl}
\label{fig:gb-ver-bit}
\end{figure}

\begin{stepbox}{20}
  Visualizar el archivo \texttt{.xdc} en el editor de \vpl, donde se observan
  las restricciones generadas a partir de los mapeos
  (Figura~\ref{fig:gb-ver-xdc}).
\end{stepbox}

\begin{figure}[H]
\centering
\includegraphics[width=1\textwidth]{generador_bitstream/20_ver_Archivo_xdc_editor_vpl.png}
\caption{Visualización del archivo de restricciones \texttt{.xdc} en el editor
de \vpl}
\label{fig:gb-ver-xdc}
\end{figure}

\section{Programación y prueba en el laboratorio remoto de FPGA's}
\label{sec:gb-fpga}

\begin{stepbox}{21}
  Usar el botón de \textbf{conexión al laboratorio remoto de FPGA's}
  \icono{lab_remoto.png} para que
  copie los archivos, incluidos el \texttt{.bit} y el \texttt{.xdc}
  (Figura~\ref{fig:gb-conexion-fpga}).
\end{stepbox}

\begin{figure}[H]
\centering
\includegraphics[width=1\textwidth]{generador_bitstream/21_conexion_la_fpga.png}
\caption{Conexión al laboratorio remoto de FPGA's y copia de los archivos
\texttt{.bit} y \texttt{.xdc}}
\label{fig:gb-conexion-fpga}
\end{figure}

\begin{stepbox}{22}
  Ver los archivos ya disponibles en el laboratorio remoto de FPGA's mediante
  \texttt{ls} en la sesión \ssh (Figura~\ref{fig:gb-ver-fpga}).
\end{stepbox}

\begin{figure}[H]
\centering
\includegraphics[width=1\textwidth]{generador_bitstream/21_ver_archvos_lab_fpga.png}
\caption{Archivos del proyecto disponibles en el laboratorio remoto de
FPGA's}
\label{fig:gb-ver-fpga}
\end{figure}

\begin{stepbox}{23}
  Usar el comando \texttt{progFPGA.sh prueba.bit} para programar la FPGA con
  el bitstream generado. Es posible observar la tarjeta a través del video del
  servidor (Figura~\ref{fig:gb-prog}).
\end{stepbox}

\begin{figure}[H]
\centering
\includegraphics[width=1\textwidth]{generador_bitstream/22_programacion_ver_servidor_video_fpga.png}
\caption{Programación de la FPGA con \texttt{progFPGA.sh} y visualización por
video del servidor}
\label{fig:gb-prog}
\end{figure}

\begin{stepbox}{24}
  Usar el comando \texttt{inputFPGA.sh xdc\_prueba.xdc} para controlar la FPGA
  a partir de las restricciones definidas (Figura~\ref{fig:gb-input}).
\end{stepbox}

\begin{figure}[H]
\centering
\includegraphics[width=1\textwidth]{generador_bitstream/23_uso_de_xdc_controlar_switches_fpga.png}
\caption{Control de la FPGA con \texttt{inputFPGA.sh} usando el archivo
\texttt{.xdc}}
\label{fig:gb-input}
\end{figure}

\begin{stepbox}{25}
  Probar la asignación de valores a los switches para comprobar que el archivo
  \texttt{.bit} efectivamente se construyó bien: al cambiar los switches, la
  FPGA responde según el diseño, validando el flujo completo
  (Figura~\ref{fig:gb-resultado}).
\end{stepbox}

\begin{figure}[H]
\centering
\includegraphics[width=1\textwidth]{generador_bitstream/24_asignacion_pines_resultado_fpga.png}
\caption{Prueba de asignación de valores en los switches y respuesta de la
FPGA que valida el bitstream generado}
\label{fig:gb-resultado}
\end{figure}


% ── Apéndices ───────────────────────────────────────────────
\appendix

\chapter{Glosario de Términos}
\label{apx:glosario}

\begin{description}
  \item[\vpl (Virtual Programming Lab)] Módulo de \moodle para editar,
        compilar, ejecutar y evaluar código directamente desde el navegador.
        En este manual se usa para diseños \vhdl.
  \item[\moodle] Plataforma de gestión del aprendizaje (\textit{LMS}) sobre la
        que se instala el plugin \vpl y se alojan los cursos y actividades.
  \item[\vhdl (VHSIC Hardware Description Language)] Lenguaje de descripción de
        hardware empleado para modelar y diseñar circuitos digitales.
  \item[FPGA (Field-Programmable Gate Array)] Dispositivo de lógica
        reconfigurable cuyas conexiones internas se programan tras la
        fabricación para implementar un circuito digital.
  \item[Lógica reconfigurable] Hardware cuyo comportamiento puede redefinirse
        mediante programación, como ocurre con las FPGA.
  \item[Bitstream] Archivo binario de configuración (\texttt{.bit}) que
        programa la FPGA con el circuito descrito en \vhdl.
  \item[Archivo \texttt{.xdc} (Xilinx Design Constraints)] Archivo de
        restricciones que asocia los puertos del diseño con pines físicos de
        la tarjeta y define sus estándares de E/S.
  \item[Testbench] Banco de pruebas en \vhdl que estimula el diseño y verifica
        sus salidas durante la simulación. \vpl puede generarlo
        automáticamente.
  \item[GHDL] Compilador y simulador de \vhdl de código abierto que \vpl usa
        para analizar y ejecutar los diseños.
  \item[GTKWave] Visor de formas de onda que muestra la evolución temporal de
        las señales obtenidas en la simulación.
  \item[Evaluación automática] Proceso por el cual \vpl ejecuta el diseño del
        alumno contra los casos de prueba del profesor y calcula una
        calificación.
  \item[Casos de prueba] Conjunto de entradas y salidas esperadas, definido en
        el archivo \texttt{vpl\_evaluate.cases}, contra el que se evalúa el
        diseño.
  \item[Entidad \textit{top}] Entidad \vhdl de nivel superior cuyos puertos se
        mapean a los pines de la tarjeta FPGA.
  \item[Mapeo de pines] Asociación de cada puerto del diseño con un recurso
        físico de la tarjeta (switch, led, botón, pin), guardada en
        \texttt{pins.yml} y traducida al archivo \texttt{.xdc}.
  \item[Board] Modelo de tarjeta FPGA destino (por ejemplo
        \texttt{nexys4ddr-a7-100t}), que determina los pines y recursos
        disponibles.
  \item[\ssh (Secure Shell)] Protocolo para abrir sesiones de línea de comandos
        cifradas en un servidor remoto.
  \item[\texttt{scp} / SFTP] Mecanismos de copia y transferencia de archivos
        cifrada sobre \ssh, usados para mover archivos entre el laboratorio
        virtual y los servidores remotos.
  \item[\texttt{genBIT.sh}] Script del servidor remoto que gestiona los
        proyectos FPGA y genera el bitstream a partir del diseño \vhdl.
  \item[\texttt{progFPGA.sh}] Script que programa la FPGA del laboratorio
        remoto cargando un archivo \texttt{.bit}.
  \item[\texttt{inputFPGA.sh}] Script que controla las entradas de la FPGA
        (por ejemplo los switches) a partir de un archivo \texttt{.xdc}.
\end{description}

\chapter{Recursos y Referencias}
\label{apx:recursos}

\begin{description}
  \item[Documentación oficial de VPL for Moodle] Características, instalación,
        configuración y solución de problemas del plugin. \\
        \url{https://vpl.dis.ulpgc.es/documentation/vpl-4.4.2/}
  \item[Sitio del proyecto VPL] Información general y descargas del Virtual
        Programming Lab. \\
        \url{https://vpl.dis.ulpgc.es/}
  \item[Moodle] Documentación de la plataforma de gestión del aprendizaje. \\
        \url{https://docs.moodle.org/}
  \item[GHDL] Documentación del compilador y simulador de \vhdl. \\
        \url{https://ghdl.github.io/ghdl/}
  \item[GTKWave] Visor de formas de onda usado en la depuración por
        simulación. \\
        \url{https://gtkwave.sourceforge.net/}
  \item[IEEE Std 1076 (VHDL)] Estándar del lenguaje \vhdl. \\
        \url{https://standards.ieee.org/ieee/1076/}
  \item[Digilent Nexys4 DDR / Nexys A7] Documentación de la tarjeta FPGA y su
        archivo maestro de restricciones (\texttt{.xdc}). \\
        \url{https://digilent.com/reference/programmable-logic/nexys-a7/start}
  \item[AMD/Xilinx – Constraints (XDC)] Guía de uso de los archivos de
        restricciones de diseño. \\
        \url{https://docs.amd.com/}
\end{description}

% ── Bibliografía ────────────────────────────────────────────
\backmatter
\bibliographystyle{plain}
% \bibliography{referencias}

\end{document}
